extends Node2D

# Color function is the format Color(#, #, #) where each value is a float from 0 to 1
# Use with .modulate to change sprite color

# __Iterating an array backwards__
# for i in range(array.size() - 1, -1, -1):
